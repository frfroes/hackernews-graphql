# import Link from "./generated/prisma.graphql"

type Query {
    info: String!
    feed: [Link!]!
    link(id: ID!): Link
}

type Mutation {
    post(data: LinkCreateInput!): Link!
    updateLink(id: ID!, data: LinkUpdateInput ): Link
    deleteLink(id: ID!): Link
}